scalar Date

type Project {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  name: String!
  description: String
}

type ProjectCursor {
  pageInfo: PageInfo!
  edges: [Project!]!
}

type Query {
  project(id: ID!): Project!
  projects(after: String, first: Int): ProjectCursor
}

type Mutation {
  createProject(name: String!, description: String): Project!
  updateProject(id: ID!, name: String, description: String): Project!
  deleteProject(id: ID!): Boolean!
}
