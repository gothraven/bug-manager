scalar Date
scalar UnsignedInt

type Status {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  name: String!
  description: String
}

type StatusCursor {
  pageInfo: PageInfo!
  edges: [Status!]!
}

type Query {
  status(id: ID!): Status!
  statuses(after: String, first: UnsignedInt): StatusCursor
}

type Mutation {
  createStatus(name: String!, description: String): Status!
  updateStatus(id: ID!, name: String, description: String): Status!
  deleteStatus(id: ID!): Boolean!
}
