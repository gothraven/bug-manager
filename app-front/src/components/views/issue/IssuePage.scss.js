import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  titleTextField: {
    "& textArea": {
      fontWeight: 500,
      fontSize: "35px",
      lineHeight: "40px"
    }
  }
}));
