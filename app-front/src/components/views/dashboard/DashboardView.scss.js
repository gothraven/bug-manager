import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  listHeaderBox: {
    display: "grid",
    gridTemplateColumns: "1fr 4fr",
    alignItems: "center"
  }
}));
